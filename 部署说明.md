# 🚀 部署说明 · Zhihui's 3D Office

欢迎收到这份 3D 学术办公室源码！这是一个用 **Three.js + React 19 + TypeScript + Vite** 搭建的 3D 互动简历网站。按下面的步骤操作，几分钟内就能在你自己的电脑上跑起来，也可以免费部署到公网。

---

## 📋 环境要求

| 项目 | 要求 |
| --- | --- |
| 操作系统 | Windows / macOS / Linux 均可 |
| Node.js | **18 及以上**（推荐 20 LTS，下文有安装方法） |
| 网络 | 安装依赖时需要联网 |

> 检查是否已装 Node：打开终端（macOS 叫"终端"、Windows 叫"PowerShell"或"CMD"），输入 `node -v`，如果显示类似 `v20.x.x` 就说明已安装，可直接跳到第 3 步。

---

## 🛠️ 第一步：安装 Node.js（如已安装请跳过）

**方式一（推荐，macOS / Linux）：** 用 nvm 安装
```bash
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.40.1/install.sh | bash
# 重启终端后执行：
nvm install 20
nvm use 20
```

**方式二（所有系统）：** 官网下载安装包
1. 打开 https://nodejs.org
2. 下载 **LTS 版本**（左侧绿色按钮）
3. 双击安装，一路"下一步"即可
4. 重新打开终端，输入 `node -v` 看到版本号即成功

---

## 📦 第二步：解压源码包

把 `3d-office-source.zip` 解压到任意位置，得到一个 `3d-office` 文件夹。

- macOS：双击压缩包即可
- Windows：右键 → 全部解压缩

---

## 📥 第三步：安装依赖（最关键的一步）

打开终端，进入项目文件夹：

```bash
cd 3d-office
```

然后安装依赖：

```bash
npm install
```

- 需要联网，会下载约 300MB 的依赖包，**请耐心等待**（快则 1 分钟，慢则 5-10 分钟）
- 安装完成后，项目里会出现一个 `node_modules` 文件夹，这是正常的，不用管它
- 如果速度很慢或报错，可换用国内镜像源后再试：
  ```bash
  npm config set registry https://registry.npmmirror.com
  npm install
  ```

---

## 🖥️ 第四步：本地运行（开发预览）

```bash
npm run dev
```

看到终端显示 `Local: http://localhost:5173` 后，用浏览器打开这个地址，就能进入你的 3D 办公室了！

> 关闭方式：在终端按 `Ctrl + C`。

---

## 🌐 第五步：构建并上线（可选）

想让对方通过网址直接访问，而不只是本地跑，按下面操作：

```bash
npm run build
```

构建完成后，项目里会出现 `dist/` 文件夹——**这就是可以直接上线的成品**。把它部署到任意静态托管平台：

**免费方案（任选其一）：**
- **Vercel**（推荐）：vercel.com 用 GitHub 账号登录 → New Project → 上传/导入 `dist` 文件夹 → Deploy，即可获得 `xxx.vercel.app` 链接
- **Netlify**：app.netlify.com → 拖拽 `dist` 文件夹到页面即可自动部署
- **Nginx / 服务器**：把 `dist` 文件夹里的内容放到网站根目录即可

---

## 🕹️ 玩法操作

| 操作 | 效果 |
| --- | --- |
| 拖拽（按住左键拖动） | 环绕旋转视角 |
| 右键拖动 | 平移视角 |
| 双击 | 聚焦某个物件 |
| 单击 | 与物件互动（如开/关台灯） |
| 右上角按钮 | 切换 英文 / 中文 |

---

## ❓ 常见问题

**Q1：`npm install` 报错，一堆红色文字？**
先看错误是否包含 `ENOENT` / `EACCES` / `node-gyp`。最常见原因是 Node 版本不对或网络问题：
1. 确认 `node -v` 是 18 以上
2. 换国内镜像源（见第三步）
3. 删除 `node_modules` 和 `package-lock.json` 后重新 `npm install`

**Q2：`npm run dev` 后浏览器打开是空白页？**
等终端出现 `Local: http://localhost:5173` 再打开；确认打开的地址和终端显示的一致。

**Q3：端口被占用？**
在终端运行 `npm run dev -- --port 5174` 换一个端口。

**Q4：页面能打开但 3D 场景加载慢 / 部分模型没显示？**
3D 模型（GLB）和照片资源较大，首次加载需要几秒到十几秒，刷新一下即可；请确认压缩包已完整解压（资源在 `public/` 文件夹里，不要单独移动）。

---

Made with ❤️ · © 2026 Zhihui Zhang
